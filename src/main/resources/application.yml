spring:
  profiles:
    group:
      "default":
        - "dev"
        - "oauth"
  application:
    name: auth-service
  config:
    import: secret.yml

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone : http://localhost:8761/eureka