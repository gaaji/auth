spring:
  profiles:
    group:
      "default":
        - "dev"
  application:
    name: auth-service
  config:
    import:
      - secret.yml
      - oauth.yml

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone : http://localhost:8761/eureka